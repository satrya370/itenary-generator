{
  "name": "itenary-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Runtime helpers for the n8n itinerary brochure workflow.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "node scripts/validate-package.mjs",
    "test": "node --test tests/*.test.mjs",
    "render:pdf": "node scripts/render-pdf.mjs"
  },
  "dependencies": {
    "puppeteer": "25.1.0"
  }
}
